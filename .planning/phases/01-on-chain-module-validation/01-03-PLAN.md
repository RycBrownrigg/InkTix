---
phase: 01-on-chain-module-validation
plan: 03
type: execute
wave: 3
depends_on: [01-01, 01-02]
files_modified:
  - contracts/inktix/src/tests/e2e/mod.rs
  - contracts/inktix/src/tests/e2e/extended_modules.rs
  - docs/v2_plan.md
autonomous: true
requirements: [VALIDATE-01, VALIDATE-02, VALIDATE-03]
estimate:
  tokens: 55000
  raw_tokens: 55000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "`season_pass_management`, `fantasy_sports_management`, `analytics`, `artist_management`, and the reachable cross-chain surface each have one passing happy-path `#[ink_e2e::test]` against a live `ink-node` (D-04, VALIDATE-01)"
    - "The 'xcm' row of the Phase 1d checklist is satisfied by `cross_chain_management`'s reachable messages, and `src/logic/core/xcm_management.rs` is recorded as having zero `#[ink(message)]` entry points rather than silently skipped (RESEARCH.md Pitfall 2)"
    - "EDGE (VALIDATE-01 / empty, explicit): `create_season_pass_package` is exercised with a non-empty (exactly one element) `benefits` Vec; the empty-Vec input traps rather than returning `Err`, so it is recorded as a Phase 3 input-validation finding and deliberately NOT exercised here (D-06, RESEARCH.md Pitfall 4)"
    - "Every one of the 7 module groups in `docs/v2_plan.md` § '1d — Behavioral validation, module by module' carries a recorded pass/fail result naming the `#[ink_e2e::test]` function that produced it (D-03, VALIDATE-01)"
    - "`cd contracts/inktix && cargo test` reports exactly 14 passed / 0 failed after all 13 e2e tests exist (VALIDATE-02)"
    - "`cd contracts/inktix && cargo contract build --release` succeeds and the resulting `target/ink/inktix.polkavm` byte size is recorded in `docs/v2_plan.md` (Phase 1 exit gate)"
    - "`.github/workflows/ci.yml` is byte-identical to its pre-phase state — on-chain validation stays a manual/local step (D-07, VALIDATE-03)"
  prohibitions:
    - "MUST NOT record a module as validated or passing in `docs/v2_plan.md`'s Phase 1d checklist unless its `#[ink_e2e::test]` was actually executed against a running `ink-node` and reported a pass in that run — an inferred, assumed, extrapolated, or 'should work' pass is a false validation record, and this checklist is what Phase 2 and Phase 3 will trust when deciding what no longer needs re-checking"
  artifacts:
    - path: "contracts/inktix/src/tests/e2e/extended_modules.rs"
      provides: "On-chain happy-path validation for season pass, fantasy sports, analytics, artist management, and cross-chain"
      min_lines: 130
    - path: "contracts/inktix/src/tests/e2e/mod.rs"
      provides: "Declaration of the extended_modules submodule"
      contains: "pub mod extended_modules"
    - path: "docs/v2_plan.md"
      provides: "Phase 1d per-module pass/fail record, on-chain findings, blob size, and exit-gate status"
      contains: "1d — Behavioral validation, module by module"
  key_links:
    - from: "contracts/inktix/src/tests/e2e/mod.rs"
      to: "contracts/inktix/src/tests/e2e/extended_modules.rs"
      via: "`pub mod extended_modules;` declaration inside the e2e module root"
      pattern: "pub mod extended_modules"
    - from: "contracts/inktix/src/tests/e2e/extended_modules.rs"
      to: "contracts/inktix/src/logic/sports/cross_chain_management.rs"
      via: "`create_cross_chain_event` / `request_cross_chain_ticket_purchase` — the reachable 'xcm' surface"
      pattern: "create_cross_chain_event"
    - from: "docs/v2_plan.md"
      to: "contracts/inktix/src/tests/e2e/"
      via: "each Phase 1d checklist row names the `#[ink_e2e::test]` function that produced its result"
      pattern: "tests::e2e::"
---

<objective>
Finish VALIDATE-01 by validating the five remaining module groups on-chain, then close
the Phase 1 exit gate: record every module's pass/fail result in `docs/v2_plan.md`
(D-03), confirm the off-chain unit suite is still green (VALIDATE-02), record the release
blob size, and confirm CI is untouched and green on `redesign/polkadot-hub`
(VALIDATE-03, D-07).

Two of this group's read-back getters are permanently dead reads — no
`#[ink(message)]` anywhere writes `storage.chain_connectivity` or
`storage.fantasy_leaderboards`. That is not a reason to skip either module: both have a
genuine on-chain round-trip proof through a *dependent write* instead (a second
extrinsic that internally looks up what the first wrote and would revert the whole call
if it were missing). Task 1 uses that proof and records the dead reads as findings.

Purpose: `docs/v2_plan.md` is this project's living source of truth per
`.claude/CLAUDE.md`, and its Phase 1d checklist is what Phase 2 will consult when
choosing which validations to re-run against Passet Hub. A checklist that is inferred
rather than executed would silently mislead two downstream phases.
Output: five more passing on-chain tests, a fully recorded Phase 1d checklist, and a
closed exit gate.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-on-chain-module-validation/01-CONTEXT.md
@.planning/phases/01-on-chain-module-validation/01-RESEARCH.md
@.planning/phases/01-on-chain-module-validation/01-PATTERNS.md
@.planning/phases/01-on-chain-module-validation/01-01-SUMMARY.md
@.planning/phases/01-on-chain-module-validation/01-02-SUMMARY.md
@docs/v2_plan.md
@.github/workflows/ci.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Validate season pass, fantasy sports, analytics, artist management, and the reachable cross-chain surface on-chain</name>
  <precondition>An `ink-node` process is accepting websocket connections on 127.0.0.1:9944, started with `./scripts/dev-node.sh` and left running for the whole session (D-02). Verify read-only, e.g. `nc -z 127.0.0.1 9944`.</precondition>
  <read_first>
    - contracts/inktix/src/tests/e2e/mod.rs (the file being modified — current submodule declarations and the `E2EResult` alias)
    - contracts/inktix/src/tests/e2e/core_modules.rs (the harness idiom to reuse: `InkTixRef::new()`, `client.instantiate("inktix", &ink_e2e::alice(), ..)`, `contract.call_builder::<InkTix>()`, `.submit().await` then `.return_value()` then unwrap the inner `Result`)
    - contracts/inktix/src/lib.rs lines 336-410 (`create_season_pass_package`, `purchase_season_pass`, `get_user_season_passes`, `create_fantasy_league`, `join_fantasy_league`, `create_fantasy_team`, `get_fantasy_leaderboard`) and lines 486-570 (`create_cross_chain_event`, `request_cross_chain_ticket_purchase`, `get_chain_connectivity`, `generate_analytics_report`, `get_analytics_report`, `register_artist`, `verify_artist`, `get_artist`)
    - contracts/inktix/src/logic/sports/season_pass_management.rs — `create_season_pass_package` performs `benefits[0].clone()` with no length check, and does NOT look up `team_id` or `season_id`
    - contracts/inktix/src/logic/sports/fantasy_sports_management.rs — `join_fantasy_league` and `create_fantasy_team` both begin with `storage.fantasy_leagues.get(league_id).ok_or("League not found")?`
    - contracts/inktix/src/logic/sports/cross_chain_management.rs — `create_cross_chain_event` and `request_cross_chain_ticket_purchase` both begin with `storage.events.get(event_id).ok_or("Event not found")?`
    - contracts/inktix/src/types/sports/season_pass.rs — the `SeasonPassBenefits` field list
    - contracts/inktix/src/types/sports/analytics.rs — the `ReportType` variants
    - .planning/phases/01-on-chain-module-validation/01-RESEARCH.md § "Common Pitfalls → Pitfall 2" and "Pitfall 4"
  </read_first>
  <files>contracts/inktix/src/tests/e2e/mod.rs, contracts/inktix/src/tests/e2e/extended_modules.rs</files>
  <action>
Add `pub mod extended_modules;` to `contracts/inktix/src/tests/e2e/mod.rs` next to the
existing `pub mod core_modules;` and `pub mod risk_tier;`. Create
`contracts/inktix/src/tests/e2e/extended_modules.rs` with the same import header the
other e2e files use, and write five `#[ink_e2e::test]` functions. Each does its own
`instantiate` as `&ink_e2e::alice()` (the deployer is therefore the owner) and obtains
any needed on-chain `Address` from `get_owner()` rather than from a keypair conversion.
Every confirming read uses `.submit()`, not `.dry_run()`.

**`season_pass_package_and_purchase_round_trip`** (D-04 happy path):
`create_season_pass_package("2026 Full Season".to_string(), 1u32, 1u32,
1_000_000_000_000_000_000u128, CurrencyId::DOT, 0u128, vec![ /* exactly one
SeasonPassBenefits */ ])` as alice (owner-gated) → `package_id`, assert it equals 1. The
`benefits` Vec must contain **exactly one** element: the implementation does
`benefits[0].clone()` with no bounds check, so an empty Vec traps the call rather than
returning `Err` — that is a Phase 3 input-validation item (D-06) to be recorded in Task
2, never exercised here. `team_id` and `season_id` are passed as `1` and are not looked
up by the implementation, so no `register_team` setup is required. Then
`purchase_season_pass(package_id)` as alice (not owner-gated; it reads
`self.env().caller()`) → `pass_id`, assert it equals 1. Then `get_owner()` →
`owner_addr` and `get_user_season_passes(owner_addr)` → assert the returned `Vec<u32>`
has length exactly 1 and element 0 equals `pass_id`.

**`fantasy_league_join_and_team_round_trip`** (D-04 happy path):
`create_fantasy_league("InkTix League".to_string(), "Phase 1 validation
league".to_string(), 10u32, 0u128, CurrencyId::DOT)` as `&ink_e2e::bob()` (not
owner-gated) → `league_id`, assert it equals 1. Then `join_fantasy_league(league_id)` as
`&ink_e2e::charlie()` → assert the returned value equals `league_id`. Then
`create_fantasy_team(league_id, "Team Charlie".to_string())` as charlie →
`fantasy_team_id`, assert it equals 1.

The round-trip proof for this module is the dependent write, not a getter: both
`join_fantasy_league` and `create_fantasy_team` begin with
`storage.fantasy_leagues.get(league_id).ok_or("League not found")?`, and an `Err` from
any `#[ink(message)]` reverts the entire extrinsic (RESEARCH.md Pitfall 1). Their
success from a *different account in a later extrinsic* is therefore direct proof that
the league bob wrote persisted to on-chain storage. State this reasoning in a comment.

Finally call `get_fantasy_leaderboard(league_id)` and assert it returns `None`, with a
comment recording why: `storage.fantasy_leaderboards` has no writer anywhere in the
contract, so this getter is a permanently dead read. Task 2 records it as a finding.

**`analytics_report_generate_and_read_round_trip`** (D-04 happy path):
`generate_analytics_report(ReportType::Daily, 0u64, u64::MAX)` as alice (owner-gated) →
`report_id`, assert it equals 1. Then `get_analytics_report(report_id)` → assert it is
`Some(..)`, that the report's id field equals `report_id`, and that its report-type
field equals `ReportType::Daily`. Read the actual `AnalyticsReport` field names from the
type definition before writing the assertions.

**`artist_register_and_verify_round_trip`** (D-04 happy path):
`register_artist("Taylor Swift".to_string())` as `&ink_e2e::bob()` (not owner-gated) →
`artist_id`, assert it equals 1. `get_artist(artist_id)` → assert `Some(..)` with `name`
equal to "Taylor Swift" and `verified` equal to `false`. Then `verify_artist(artist_id)`
as `&ink_e2e::alice()` (owner-gated — calling it as bob would revert). Then
`get_artist(artist_id)` again → assert `verified` is now `true`. The before/after pair on
the same field is what makes this discriminating rather than a bare existence check.

**`cross_chain_event_and_request_round_trip`** (D-04 happy path; this is the checklist's
"xcm" row): `register_venue("Crypto Arena".to_string(), 20000, "Los
Angeles".to_string(), VenueType::Arena)` as alice → `venue_id`; `create_event("Cross
Chain Event".to_string(), venue_id, 1u64, 20000, 1_000_000_000_000_000_000u128,
EventCategory::Generic)` as alice → `event_id`. Then
`create_cross_chain_event(event_id, BlockchainNetwork::Moonbeam)` as alice (owner-gated)
→ `cross_chain_event_id`, assert it equals 1. Then
`request_cross_chain_ticket_purchase(event_id, BlockchainNetwork::Moonbeam, seat,
CurrencyId::DOT)` as `&ink_e2e::bob()` (not owner-gated) → `request_id`, assert it
equals 1, building the same `Seat` literal the other e2e files use
(`SeatType::GeneralAdmission`, `AccessLevel::Standard`, `price_multiplier: 0`). As with
fantasy, both cross-chain messages open with
`storage.events.get(event_id).ok_or("Event not found")?`, so their success proves the
event round-tripped. Finally call `get_chain_connectivity(BlockchainNetwork::Moonbeam)`
and assert it returns `None`, with a comment recording that
`storage.chain_connectivity` has no writer anywhere in the contract.

Do **not** write any test against `contracts/inktix/src/logic/core/xcm_management.rs` —
it is compiled but unreachable: it is absent from `lib.rs`'s
`use crate::logic::core::{..}` import list and no `#[ink(message)]` calls into it, so
there is no way to invoke it on-chain at all. Task 2 records that fact against the
checklist's "xcm" row.
  </action>
  <acceptance_criteria>
    - `contracts/inktix/src/tests/e2e/mod.rs` declares all three submodules: `core_modules`, `risk_tier`, `extended_modules`
    - `contracts/inktix/src/tests/e2e/extended_modules.rs` defines exactly five `#[ink_e2e::test]` functions named `season_pass_package_and_purchase_round_trip`, `fantasy_league_join_and_team_round_trip`, `analytics_report_generate_and_read_round_trip`, `artist_register_and_verify_round_trip`, `cross_chain_event_and_request_round_trip`
    - The `create_season_pass_package` call passes a `benefits` Vec with exactly one element
    - `artist_register_and_verify_round_trip` asserts `verified == false` before `verify_artist` and `verified == true` after
    - `grep -c 'create_cross_chain_event' contracts/inktix/src/tests/e2e/extended_modules.rs` reports at least 1
    - The `tests::e2e::extended_modules` filter run reports `running 5 tests` and `test result: ok. 5 passed; 0 failed`
    - The whole e2e suite run reports `running 13 tests` and `test result: ok. 13 passed; 0 failed`
  </acceptance_criteria>
  <verify>
    <automated>cd contracts/inktix &amp;&amp; CONTRACTS_NODE_URL=ws://127.0.0.1:9944 cargo test --features e2e-tests tests::e2e::extended_modules -- --test-threads=1 --nocapture</automated>
    <fails_when>non-zero exit, or the output contains `running 0 tests` (the name filter matched nothing), or the summary line is not `test result: ok. 5 passed; 0 failed`</fails_when>
    <automated>cd contracts/inktix &amp;&amp; CONTRACTS_NODE_URL=ws://127.0.0.1:9944 cargo test --features e2e-tests tests::e2e -- --test-threads=1</automated>
    <fails_when>non-zero exit, or the output contains `running 0 tests`, or the summary line is not `test result: ok. 13 passed; 0 failed`</fails_when>
  </verify>
  <done>All five remaining Phase 1d module groups pass on-chain, the full e2e suite is 13/13 green, and the two dead-read getters are asserted as `None` with the reason recorded in-source.</done>
</task>

<task type="auto">
  <name>Task 2: Record the Phase 1d per-module results and findings in docs/v2_plan.md and close the Phase 1 exit gate</name>
  <precondition>`.planning/phases/01-on-chain-module-validation/01-02-SUMMARY.md` exists and reports the observed `chain_now_ms` / `host_now_ms` pair from plan 01-02 Task 1 (needed for the A1 finding below).</precondition>
  <read_first>
    - docs/v2_plan.md — the whole file, and specifically line 18 (the Phase 1 status row of the summary table), § "1d — Behavioral validation, module by module" (the 7-item numbered module list and the "Exit gate for Phase 1" paragraph), and line 343 (the previously recorded `322.3K original → 103.6K` blob-size figure)
    - .planning/phases/01-on-chain-module-validation/01-01-SUMMARY.md and 01-02-SUMMARY.md (the actual executed results and any findings to transcribe — this is the source of truth for what passed)
    - .github/workflows/ci.yml (to confirm it is unmodified; the `contracts` job runs `cargo test` then `cargo contract build` with no `--features e2e-tests` flag)
    - .planning/phases/01-on-chain-module-validation/01-CONTEXT.md § D-03 and D-07
  </read_first>
  <files>docs/v2_plan.md</files>
  <action>
Record results **in place** in `docs/v2_plan.md` (D-03 — do not create a separate
VALIDATION.md; this file is the project's living source of truth per `.claude/CLAUDE.md`).

**Step 1 — run the gate before writing anything.** Run all three, capture the real
output, and use the real numbers below:
- `cd contracts/inktix && CONTRACTS_NODE_URL=ws://127.0.0.1:9944 cargo test --features e2e-tests tests::e2e -- --test-threads=1` (all 13 on-chain tests)
- `cd contracts/inktix && cargo test` (the off-chain suite — VALIDATE-02)
- `cd contracts/inktix && cargo contract build --release` then read the byte size of
  `contracts/inktix/target/ink/inktix.polkavm`

Only a module whose test actually ran and passed in this session may be recorded as
passing. Do not transcribe an expectation.

**Step 2 — rewrite the § "1d" module list into a results table.** Keep the existing
seven numbered module groups and their order (that order is the locked dependency chain).
For each group add: the result (pass/fail), the `#[ink_e2e::test]` function name that
produced it (fully qualified, e.g. `tests::e2e::core_modules::venue_and_event_round_trip`),
and a one-line note. The mapping is:

1. `venue_management` / `event_management` → `tests::e2e::core_modules::venue_and_event_round_trip`
2. `ticket_management` + `user_tickets` → `tests::e2e::core_modules::ticket_purchase_and_user_tickets_round_trip`
3. `pricing` → `tests::e2e::risk_tier::pricing_time_multiplier_under_manual_seal`
4. `nft_management` → `tests::e2e::core_modules::nft_mint_and_verify_round_trip`
5. `anti_scalping` → `tests::e2e::risk_tier::anti_scalping_blacklist_vec_survives_repeated_mutation`
6. `loyalty` / `advanced_team_loyalty` → `tests::e2e::risk_tier::loyalty_composite_key_accumulates_across_double_stake`
7. `season_pass_management`, `fantasy_sports_management`, `analytics`, `artist_management`, `xcm` → the five `tests::e2e::extended_modules::*` functions, one row each

Row 7's `xcm_management` entry must be re-pointed explicitly, not silently reinterpreted:
state that the checklist's "xcm" item is satisfied by `cross_chain_management`, and why
(finding F1 below).

**Step 3 — add a findings subsection** under § 1d, recording each item with the file and
symbol it concerns:
- **F1** — `src/logic/core/xcm_management.rs` is compiled but unreachable: it is absent
  from `lib.rs`'s `use crate::logic::core::{..}` import list and no `#[ink(message)]`
  calls into it, so `send_xcm_message` / `get_xcm_analytics` /
  `update_xcm_chain_connectivity` cannot be invoked on-chain at all. The reachable
  cross-chain surface is `src/logic/sports/cross_chain_management.rs`. Flag it as dead
  code to remove or wire up in a later phase.
- **F2** — `get_chain_connectivity` is a permanently dead read: no message writes
  `storage.chain_connectivity`, so it always returns `None`.
- **F3** — `get_fantasy_leaderboard` is a permanently dead read: no message writes
  `storage.fantasy_leaderboards`, so it always returns `None`.
- **F4** — `create_season_pass_package` performs `benefits[0].clone()` with no length
  check, so an empty `benefits` Vec traps the call instead of returning `Err`. Assign it
  to Phase 3, which owns input-validation hardening (D-06).
- **F5** — manual-seal block-timestamp semantics: record the observed `chain_now_ms` and
  `host_now_ms` values from `01-02-SUMMARY.md` and state whether `ink-node`'s
  manually-sealed blocks carry real wall-clock time. This resolves the assumption
  RESEARCH.md logged as A1 and is exactly the "surprises" item § 1d flagged in advance.
- **F6** — `scripts/dev-node.sh` resolved its binary at a fixed depth
  (`.tooling/ink-node/ink-node`) that the mac-universal release asset does not produce,
  causing a needless ~300MB re-download; it now resolves the binary at any depth and
  supports `--resolve-only`.

**Step 4 — record the exit-gate numbers.** Under the existing "Exit gate for Phase 1"
paragraph, record: the on-chain result (`13/13` e2e tests passing, or the real figure),
the off-chain result (`14/14` inline unit tests passing, or the real figure), the
`cargo contract build --release` blob size in bytes and KB for
`target/ink/inktix.polkavm` compared against the previously recorded 103.6K, and the CI
status on `redesign/polkadot-hub`.

**Step 5 — record the CI scope decision (D-07).** Add a short note stating that the
`ink_e2e` on-chain suite is a manual/local step and is deliberately NOT wired into
`.github/workflows/ci.yml` — the CI `contracts` job never passes `--features e2e-tests`,
so the new tests compile out of it entirely, which is what keeps VALIDATE-02 and
VALIDATE-03 true by construction. Include the two-terminal run recipe: `./scripts/dev-node.sh`
in one terminal, and
`cd contracts/inktix && CONTRACTS_NODE_URL=ws://127.0.0.1:9944 cargo test --features e2e-tests tests::e2e -- --test-threads=1`
in the other. Note that standing an `ink-node` up in CI is deferred infrastructure work.

**Step 6 — update the Phase 1 status row** at `docs/v2_plan.md` line 18, replacing
"1d remaining" / "not yet run" with the completed status and the recorded blob size.

Do not modify `.github/workflows/ci.yml`, `contracts/inktix/Cargo.toml`, or
`contracts/inktix/rust-toolchain.toml` in this task.
  </action>
  <acceptance_criteria>
    - `docs/v2_plan.md` § "1d — Behavioral validation, module by module" lists all 7 module groups, each with an explicit pass/fail result and a fully-qualified `tests::e2e::` function name
    - `grep -c 'tests::e2e::' docs/v2_plan.md` reports at least 13 (one per e2e test function)
    - `docs/v2_plan.md` contains findings F1 through F6, each naming the specific file and symbol it concerns
    - The recorded F5 entry contains the actual numeric `chain_now_ms` and `host_now_ms` values observed in plan 01-02, not a placeholder
    - The "Exit gate for Phase 1" paragraph records the on-chain pass count, the off-chain pass count, the `target/ink/inktix.polkavm` byte size, and the CI status
    - `docs/v2_plan.md` line 18's Phase 1 status row no longer reads "1d remaining" or "not yet run"
    - `git diff --name-only` after this task shows `docs/v2_plan.md` and does NOT show `.github/workflows/ci.yml`, `contracts/inktix/Cargo.toml`, or `contracts/inktix/rust-toolchain.toml`
    - `cd contracts/inktix && cargo test` reports `test result: ok. 14 passed; 0 failed`
    - `cd contracts/inktix && cargo contract build --release` succeeds and produces a non-empty `target/ink/inktix.polkavm`
  </acceptance_criteria>
  <verify>
    <automated>cd contracts/inktix &amp;&amp; cargo test</automated>
    <fails_when>non-zero exit, or the summary line is not `test result: ok. 14 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`</fails_when>
    <automated>cd contracts/inktix &amp;&amp; cargo contract build --release &amp;&amp; wc -c &lt; target/ink/inktix.polkavm</automated>
    <fails_when>non-zero exit, or `target/ink/inktix.polkavm` is absent, or the printed byte count is `0`</fails_when>
    <automated>git diff --name-only -- .github/workflows/ci.yml contracts/inktix/Cargo.toml contracts/inktix/rust-toolchain.toml | wc -l</automated>
    <fails_when>the printed count is anything other than `0` — any non-zero value means CI config or a pinned-version file was modified, violating D-07 and prohibition P3</fails_when>
    <automated>grep -c 'tests::e2e::' docs/v2_plan.md</automated>
    <fails_when>non-zero exit (grep found no matches at all), or the printed count is less than `13` — fewer than one recorded row per e2e test function means the checklist is incomplete</fails_when>
    <human-check>Confirm the GitHub Actions run for the latest push to `redesign/polkadot-hub` reports the `contracts` job as green (`gh run list --branch redesign/polkadot-hub --limit 1` or the Actions tab). The two steps that job runs — `cargo test` and `cargo contract build`, both from `working-directory: contracts/inktix`, with no `--features e2e-tests` — are proven locally by the automated commands above, but only the remote run settles VALIDATE-03. See `## Flagged Assumptions` below for what "green" is being assumed to mean.</human-check>
  </verify>
  <done>`docs/v2_plan.md`'s Phase 1d checklist records an executed pass/fail plus the producing test name for all 7 module groups, findings F1-F6 are written down, the exit-gate numbers (13/13 on-chain, 14/14 off-chain, blob size, CI status) are recorded, and no CI or pinned-version file was touched.</done>
</task>

</tasks>

<flagged_assumptions>
## Flagged Assumptions

**A-01 — VALIDATE-03 edge probe: `unclassified`, carried forward unresolved.**

The deterministic edge probe classified VALIDATE-03 ("CI is green on
`redesign/polkadot-hub`") as `unclassified` and proposed no applicable edge category.
Per the spec-less fallback rules an `unclassified` row is never auto-resolved and never
auto-dismissed, so it is surfaced here as an explicit assumption rather than converted
into a `must_haves` truth.

**What is being assumed:** that "CI is green" means the existing GitHub Actions
`contracts` job — pinned `nightly-2026-01-15`, `cargo install --locked --version
6.0.0-beta.2 cargo-contract`, then `cargo test` and `cargo contract build` from
`working-directory: contracts/inktix` — completes successfully on a push to
`redesign/polkadot-hub` carrying this phase's changes.

**What is genuinely unspecified anywhere in the source artifacts:**
- Whether the `frontend` job (untouched by this phase) counts toward "green", i.e.
  whether a green `contracts` job alongside a red `frontend` job satisfies VALIDATE-03.
- How a queued, cancelled, or skipped run is to be treated.
- Whether "green" must be observed on a push containing this phase's commits
  specifically, or merely the branch's most recent run.

**How it is handled rather than hidden:** Task 2's automated commands prove the two
commands the `contracts` job actually runs pass locally under the pinned toolchain, and
prove `.github/workflows/ci.yml` was not modified (so the job's definition is unchanged
from the state that was already green). The remote run conclusion is routed to a
`<human-check>` for a human to judge against the ambiguity above. No automated criterion
in this plan claims to settle VALIDATE-03 on its own.

**A-02 — RESEARCH.md assumption A1 (manual-seal timestamp semantics) is resolved, not
assumed, by plan 01-02 Task 1.** It is listed here only for traceability: the value
observed on-chain is recorded as finding F5 in `docs/v2_plan.md` by Task 2 above. If
01-02 Task 1's wall-clock assertion failed, F5 records the actual semantics and the
substituted assertion — see prohibition P2 in `01-02-PLAN.md` for why that substitution
is permitted and assertion-loosening is not.
</flagged_assumptions>

<artifacts_this_phase_produces>
## Artifacts this phase produces

Net-new symbols created by **this plan** (nothing below exists in the repository today):

- **New file:** `contracts/inktix/src/tests/e2e/extended_modules.rs`
- **New Rust module:** `crate::tests::e2e::extended_modules` (declared in
  `contracts/inktix/src/tests/e2e/mod.rs` as `pub mod extended_modules;`)
- **New `#[ink_e2e::test]` functions:**
  - `tests::e2e::extended_modules::season_pass_package_and_purchase_round_trip`
  - `tests::e2e::extended_modules::fantasy_league_join_and_team_round_trip`
  - `tests::e2e::extended_modules::analytics_report_generate_and_read_round_trip`
  - `tests::e2e::extended_modules::artist_register_and_verify_round_trip`
  - `tests::e2e::extended_modules::cross_chain_event_and_request_round_trip`
- **New documentation sections** in the existing `docs/v2_plan.md`: a per-module Phase 1d
  results table, a findings subsection F1-F6, exit-gate figures, and the D-07 CI-scope
  note with the two-terminal run recipe.

No new CLI flags, no new Cargo features, no new config entries.
`contracts/inktix/Cargo.toml`, `contracts/inktix/rust-toolchain.toml`, and
`.github/workflows/ci.yml` are **not** modified by this plan.

The full phase-wide artifact list (all 13 test functions, the `E2EResult` alias, the new
`scripts/dev-node.sh` `--resolve-only` flag) is in
`.planning/phases/01-on-chain-module-validation/01-01-PLAN.md`.
</artifacts_this_phase_produces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test process → local `ink-node` RPC (ws://127.0.0.1:9944) | Unauthenticated localhost websocket carrying signed extrinsics |
| test keypair → contract message dispatch | Owner-gated (`verify_artist`, `generate_analytics_report`, `create_cross_chain_event`, `create_season_pass_package`) vs. open messages diverge on the caller H160 |
| executed test results → `docs/v2_plan.md` | The human-readable validation record that Phase 2 and Phase 3 will trust; a false entry here silently misdirects two later phases |
| repository → GitHub Actions runner | CI fetches the branch and executes the pinned toolchain against it |

## STRIDE Threat Register (ASVS L1, block on `high`)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Repudiation | `docs/v2_plan.md` Phase 1d checklist — a module recorded as passing without an executed run | high | mitigate | Prohibition P1 in this plan's `must_haves.prohibitions` forbids it outright; Task 2's action mandates running the gate BEFORE writing anything and using the captured numbers; acceptance criteria require a fully-qualified `tests::e2e::` function name on every row (`grep -c 'tests::e2e::' docs/v2_plan.md` at least 13) and real numeric values in F5, so an inferred entry fails its own criterion |
| T-01-10 | Tampering | `.github/workflows/ci.yml` silently modified to make VALIDATE-03 pass | high | mitigate | `.github/workflows/ci.yml` is in no plan's `files_modified`; Task 2 carries an automated `git diff --name-only` gate over the CI file plus both pinned-version files whose `<fails_when>` treats any non-zero count as failure |
| T-01-11 | Information Disclosure | Test source committing account secret material | medium | mitigate | Signing uses only `ink_e2e`'s public dev keyring (`alice` / `bob` / `charlie`); the negative-grep criterion established on plan 01-01 Task 2 covers the whole `contracts/inktix/src/tests/e2e/` directory, including this plan's new file |
| T-01-12 | Elevation of Privilege | `ensure_owner()` on `verify_artist`, `generate_analytics_report`, `create_cross_chain_event`, `create_season_pass_package` | medium | accept | Pre-existing control at `contracts/inktix/src/lib.rs:80-85`, unchanged by this plan. The owner-caller happy path is exercised and, in `artist_register_and_verify_round_trip`, the owner-vs-non-owner split is exercised positively (bob registers, alice verifies). Non-owner-rejection testing is deferred by D-06 to Phase 3. This plan adds tests and documentation, not contract logic |
| T-01-13 | Denial of Service | Shared local port 9944 — a stale or absent `ink-node` process | low | accept | Localhost dev only. Task 1 carries a `<precondition>` the executor asserts read-only before running, so an absent node halts the task rather than producing a misleading "0 tests" pass; every `<fails_when>` also treats `running 0 tests` as failure |
| T-01-SC | Tampering | npm/pip/cargo installs | low | accept | Zero package installs occur in this plan; `contracts/inktix/Cargo.toml` is not in `files_modified` and is additionally guarded by Task 2's `git diff` gate. See `01-01-PLAN.md` for the phase-wide rationale |

No `high`-severity threat is left un-mitigated, so the `security_block_on: "high"` gate
is satisfied.
</threat_model>

<verification>
1. `cd contracts/inktix && CONTRACTS_NODE_URL=ws://127.0.0.1:9944 cargo test --features e2e-tests tests::e2e -- --test-threads=1` reports `running 13 tests` and `13 passed; 0 failed`.
2. `cd contracts/inktix && cargo test` reports `test result: ok. 14 passed; 0 failed`.
3. `cd contracts/inktix && cargo test -- --list` yields exactly 14 `: test` lines.
4. `cd contracts/inktix && cargo contract build --release` succeeds and `target/ink/inktix.polkavm` is non-empty.
5. `git diff --name-only -- .github/workflows/ci.yml contracts/inktix/Cargo.toml contracts/inktix/rust-toolchain.toml` is empty.
6. `docs/v2_plan.md` records all 7 module groups with executed results plus findings F1-F6 and the exit-gate figures.
7. Human-check: the GitHub Actions `contracts` job is green on `redesign/polkadot-hub`.
</verification>

<success_criteria>
- Five `#[ink_e2e::test]` functions exist in `contracts/inktix/src/tests/e2e/extended_modules.rs` and all pass against a live `ink-node`; the full e2e suite is 13/13.
- All 7 Phase 1d module groups carry an executed pass/fail result in `docs/v2_plan.md`, each naming the test function that produced it.
- Findings F1-F6 are recorded, including the numeric A1 resolution and the two dead-read getters.
- The release blob size is recorded and compared against the previous 103.6K figure.
- The 14 inline unit tests are green (VALIDATE-02) and `.github/workflows/ci.yml` is unmodified with the `contracts` job green on `redesign/polkadot-hub` (VALIDATE-03, D-07).
</success_criteria>

<output>
Create `.planning/phases/01-on-chain-module-validation/01-03-SUMMARY.md` when done.
</output>
