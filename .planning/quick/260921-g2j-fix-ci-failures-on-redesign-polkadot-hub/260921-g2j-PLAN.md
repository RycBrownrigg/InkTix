---
phase: quick-260921-g2j
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - contracts/inktix/src/lib.rs
  - contracts/inktix/src/logic/core/event_management.rs
  - contracts/inktix/src/logic/core/ticket_management.rs
  - contracts/inktix/src/logic/core/venue_management.rs
  - contracts/inktix/src/logic/sports/cross_chain_management.rs
  - contracts/inktix/src/logic/sports/fantasy_sports_management.rs
  - contracts/inktix/src/logic/sports/loyalty.rs
  - frontend/package-lock.json
autonomous: true
requirements: [CI-GREEN-01]

estimate:
  tokens: 48000
  raw_tokens: 32000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`RUSTFLAGS=\"-D warnings\" cargo test` in contracts/inktix compiles clean and reports 14 passed, 0 failed"
    - "`RUSTFLAGS=\"-D warnings\" cargo contract build` in contracts/inktix succeeds"
    - "`npm ci` in frontend/ completes without EUSAGE lockfile errors"
    - "`npm run lint`, `npm run build`, and `npx vitest run` all succeed in frontend/"
    - "The two genuine logic gaps found during planning are documented in code and reported to the user, not silently suppressed"
    - "The contract diff is behavior-neutral: no on-chain logic, ID allocation, or ink! message ABI label changes"
    - "The GitHub Actions run for the pushed commit on redesign/polkadot-hub reports conclusion: success"
  artifacts:
    - contracts/inktix/src/lib.rs
    - contracts/inktix/src/logic/core/event_management.rs
    - contracts/inktix/src/logic/core/venue_management.rs
    - frontend/package-lock.json
  key_links:
    - "RUSTFLAGS=\"-D warnings\" is the exact mechanism actions-rust-lang/setup-rust-toolchain@v1 uses; setting it locally reproduces the CI gate without editing ci.yml"
    - "npm ci requires resolved entries in the lockfile `packages` map, not just dependency declarations inside other packages"
    - "ink! message parameter names become arg labels in contract metadata, which the frontend SDK consumes"
---

<objective>
Make CI green on `redesign/polkadot-hub`. Two independent, pre-existing failures block the
first-ever push of this branch:

1. **Smart Contracts job** — `actions-rust-lang/setup-rust-toolchain@v1` defaults to
   `RUSTFLAGS="-D warnings"`, promoting 19 pre-existing warnings in `contracts/inktix` to
   hard compile errors.
2. **Frontend job** — `npm ci` fails with `EUSAGE` because `frontend/package-lock.json` has
   no resolved entries for `@polkadot-api/substrate-client@0.1.4` and `smoldot@2.0.26`.

Purpose: unblock the branch's CI so the in-progress Phase 1 (On-Chain Module Validation)
work has a working signal to land against. Neither failure is caused by Phase 1.

Output: a behavior-neutral contract lint cleanup, a resynced frontend lockfile, a pushed
commit, and a confirmed-green Actions run.
</objective>

<execution_context>
@~/.claude/gsd-core/workflows/execute-plan.md
@~/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@.claude/CLAUDE.md

Failing CI run: https://github.com/RycBrownrigg/InkTix/actions/runs/35618789359
Repo: RycBrownrigg/InkTix — branch `redesign/polkadot-hub`
</context>

<prohibitions>
These files are pinned/protected. Do NOT modify them under any circumstance — the fix must
work *within* the `-D warnings` constraint, never by loosening it:

- `.github/workflows/ci.yml` (adding `RUSTFLAGS: ""` or `env:` overrides is forbidden)
- `contracts/inktix/Cargo.toml` (no `[lints]` table, no profile flags)
- `contracts/inktix/rust-toolchain.toml` (hard toolchain pin per project CLAUDE.md)

Also forbidden:
- Crate-level `#![allow(...)]` in `lib.rs` when a narrower scope works
- A blanket `#![allow(unused)]` / `#![allow(dead_code)]` anywhere
- `.cargo/config.toml` RUSTFLAGS overrides (same effect as editing ci.yml)
</prohibitions>

<planning_findings>
Planning already read every flagged site. These findings are the baseline — Task 1 must
confirm them by reading the code, not accept them blindly.

**Genuinely dead imports (remove outright, 7 sites):**
| File | Line | Import |
|---|---|---|
| `logic/core/ticket_management.rs` | 16 | `ink::prelude::vec::Vec` |
| `logic/sports/cross_chain_management.rs` | 14 | `ink::prelude::vec::Vec` |
| `logic/sports/fantasy_sports_management.rs` | 15 | `ink::prelude::vec::Vec` |
| `logic/sports/loyalty.rs` | 15 | `ink::prelude::vec::Vec` |
| `logic/sports/loyalty.rs` | 17 | `ink::prelude::vec` |
| `logic/core/event_management.rs` | 11 | `ink::primitives::Address` |
| `lib.rs` | 51 | `crate::types::*` |

**CRITICAL distinction:** `cross_chain_management.rs` and `fantasy_sports_management.rs`
DO use the `vec![]` macro, but the dead import is the `Vec` *type* (`ink::prelude::vec::Vec`),
which is separate from the `vec` *macro* import (`ink::prelude::vec`). Remove only the type
import in those two files. `loyalty.rs` uses neither, so both of its imports go.
`lib.rs:51` is redundant because the module already does `use super::*` (line 39) and the
crate root re-exports via `pub use types::*` (line 26).

**Pure naming noise (underscore-prefix is correct):**
- `event_management.rs:48` — `home_team`. The binding exists for its *side effect* (the
  `.ok_or("Home team not found")?` existence check). The adjacent `_away_team` (line 49) and
  `_season` (line 50) already use exactly this convention. Renaming to `_home_team` makes it
  consistent.
- `venue_management.rs:118-124` — parameters of an internal (non-`#[ink(message)]`) helper.
  Underscore is safe here; see the logic-gap note below for why they are unused.

**GENUINE LOGIC GAPS — must be documented, not silently suppressed:**

*Gap A — `VenueManagement::purchase_parking_pass` is an unimplemented stub*
(`logic/core/venue_management.rs:116-129`). It accepts `buyer`, `pass_type`, `valid_from`,
`valid_until`, `lot_name`, `currency`, then discards all six, constructs no `ParkingPass`,
and writes nothing to storage. Planning confirmed all the infrastructure it needs already
exists: the `ParkingPass` struct (`types/core_types/venue.rs:483`), and the
`parking_passes`, `user_parking_passes`, `venue_parking_passes` mappings
(`storage/contract_storage.rs:135,140,143`). It also calls `storage.get_next_id("venue")`
where a dedicated `"parking_pass"` counter exists (`contract_storage.rs:360`) — so it both
stores nothing AND increments the wrong counter. Contrast with the sibling
`purchase_concession_credits` (lines 132-155), which correctly builds and inserts its record.

*Gap B — `InkTix::purchase_concession_credits` ignores the caller's currency*
(`lib.rs:463-471`). The `#[ink(message)]` accepts `currency: CurrencyId` from the caller,
then hardcodes `"DOT".to_string()` in the downstream call (line 469). The user's chosen
currency is silently dropped. `CurrencyId` (`types/core_types/currency.rs:12`) has five
variants and no `Display`/`to_string` impl, so wiring it needs a small conversion that does
not yet exist. Related: `lib.rs:450` `purchase_parking_pass` accepts `event_id` and never
forwards it (unsurprising, since Gap A's callee ignores everything).

*Possible Gap C — unwired sport validation* (`event_management.rs:44`). `sport_type` is
destructured from `EventCategory::Sports` and never used. Both `Team` and `Season` carry
their own `sport_type` field, and the arm already fetches the home team, away team, and
season — so a consistency check (event/team/season sports agree) looks like it was intended
and never written. Lower confidence than A and B; document as a question, not a defect.
</planning_findings>

<tasks>

<task type="auto">
  <name>Task 1: Clear all contract warnings under -D warnings, behavior-neutrally</name>
  <files>contracts/inktix/src/lib.rs, contracts/inktix/src/logic/core/event_management.rs, contracts/inktix/src/logic/core/ticket_management.rs, contracts/inktix/src/logic/core/venue_management.rs, contracts/inktix/src/logic/sports/cross_chain_management.rs, contracts/inktix/src/logic/sports/fantasy_sports_management.rs, contracts/inktix/src/logic/sports/loyalty.rs</files>
  <read_first>
    Before changing anything, read these three regions and confirm or correct the
    `<planning_findings>` above. Do not skip this — two of these sites are real defects and
    the whole point of this task is to not paper over them:
    - `contracts/inktix/src/logic/core/event_management.rs` lines 40-60 (the Sports match arm)
    - `contracts/inktix/src/logic/core/venue_management.rs` lines 110-160 (compare the
      parking-pass stub against the concession-credits function directly below it)
    - `contracts/inktix/src/lib.rs` lines 445-472 (the two sports venue messages)
  </read_first>
  <action>
    Establish the ground truth first. Run `cd contracts/inktix && RUSTFLAGS="-D warnings" cargo test 2>&1 | tee /tmp/before.txt`
    and capture the full warning list. Work from that output, not from the
    `<planning_findings>` table, if the two ever disagree — the compiler is authoritative.

    **Governing constraint: this change is behavior-neutral.** Every edit is an import
    removal, an underscore rename, a scoped allow attribute, or a comment. No on-chain logic
    changes, no ID-allocation changes, no ink! message ABI label changes. The 14 unit tests
    must pass without being edited.

    1. **Dead imports.** Delete the seven imports listed in `<planning_findings>`. Respect
       the `Vec`-type vs `vec`-macro distinction called out there — removing the macro import
       from `cross_chain_management.rs` or `fantasy_sports_management.rs` will break the build.

    2. **Naming noise.** Rename `home_team` to `_home_team` at `event_management.rs:48`,
       matching the `_away_team` / `_season` convention already used on the next two lines.

    3. **Internal helper parameters.** In `venue_management.rs`, underscore-prefix the six
       unused parameters of `purchase_parking_pass` (`buyer`, `pass_type`, `valid_from`,
       `valid_until`, `lot_name`, `currency`). This function is not an `#[ink(message)]`, so
       renaming is ABI-safe.

    4. **ink! message parameters — do NOT rename.** For `lib.rs:450` (`event_id`) and
       `lib.rs:463` (`currency`), these are `#[ink(message)]` parameters whose names become
       arg labels in the generated contract metadata that the frontend SDK consumes. Renaming
       them to `_event_id` / `_currency` would change the published ABI. Instead, suppress the
       warning from inside the function body with a discard binding (`let _ = event_id;`),
       which silences `unused_variables` while preserving the public label.

    5. **Document the real gaps.** Per the planning findings, Gap A, Gap B, and Gap C are
       incomplete functionality, not naming noise. Attach a durable marker comment at each of
       the three sites. Use a consistent `TODO(v2-gap):` prefix so the markers are greppable,
       and in each one state plainly what is missing and what already exists to support it —
       for Gap A that the record is never persisted and the counter used is the venue one
       rather than the dedicated parking one; for Gap B that the caller-supplied currency is
       dropped in favour of a hardcoded value and that the enum has no string conversion yet;
       for Gap C that the destructured sport is never checked against the team and season
       sports. Keep them factual and short.

       Do NOT repair Gap A, B, or C in this task. Each is a behavior change, each is untested
       on-chain, and currency handling belongs to the upcoming payment phase. Surface them
       instead: carry all three into the task summary so the user can schedule them.

    6. **Unexpected cfg.** For the `unexpected cfg condition name: ink_abi` warning on the
       `#[ink::contract]` macro at `lib.rs:35` — this is a known false positive from the ink!
       macro's own internal cfg usage on this ink!/toolchain pair, not a typo in project code.
       Apply the narrowest scoped allow that actually works, trying in this order and stopping
       at the first that clears the warning: (a) `#[allow(unexpected_cfgs)]` placed directly
       above `#[ink::contract]`; (b) the same attribute grouped with the existing
       `#[allow(clippy::...)]` attributes just below it; (c) as a last resort, a module-inner
       attribute at the top of `pub mod inktix { ... }`. All three are narrower than a
       crate-level allow, which is forbidden. Add a one-line comment naming it as an upstream
       macro false positive so nobody later mistakes it for sloppiness.

    7. Re-run the gate and iterate until the warning list is empty. If the compiler surfaces
       warnings beyond the 19 known ones, fix them with the same behavior-neutral rules; if
       any of them cannot be fixed behavior-neutrally, stop and report rather than guessing.
  </action>
  <verify>
    <automated>cd contracts/inktix && RUSTFLAGS="-D warnings" cargo test 2>&1 | tail -30</automated>
    <automated>cd contracts/inktix && RUSTFLAGS="-D warnings" cargo contract build 2>&1 | tail -20</automated>
    <automated>cd /Volumes/WorkFiles/Projects/InkTix_hackathon_version && grep -rn "TODO(v2-gap)" contracts/inktix/src/ | wc -l</automated>
    <automated>cd /Volumes/WorkFiles/Projects/InkTix_hackathon_version && git diff --name-only -- .github/workflows/ci.yml contracts/inktix/Cargo.toml contracts/inktix/rust-toolchain.toml</automated>
  </verify>
  <done>
    - `RUSTFLAGS="-D warnings" cargo test` compiles with zero warnings and prints
      `test result: ok. 14 passed; 0 failed`.
    - `RUSTFLAGS="-D warnings" cargo contract build` succeeds.
    - The `TODO(v2-gap)` marker count is 3 (one each at the parking-pass stub, the
      concession-credits currency call site, and the Sports match arm).
    - The protected-files diff check prints nothing — none of the three pinned files changed.
    - No test file was edited; the 14 tests pass as-is.
    - `git diff contracts/` contains only import removals, underscore renames, discard
      bindings, allow attributes, and comments.
  </done>
</task>

<task type="auto">
  <name>Task 2: Resync frontend/package-lock.json so npm ci succeeds</name>
  <files>frontend/package-lock.json</files>
  <action>
    The diagnosis from planning: `package-lock.json` is `lockfileVersion: 3` and *declares*
    `@polkadot-api/substrate-client@0.1.4` (line ~1756) and `smoldot@2.0.26` (line ~4165) as
    dependencies of other `@polkadot/*` packages, but carries no resolved entries for them in
    its `packages` map. `npm ci` requires resolved entries, hence `EUSAGE`. Neither package is
    a new direct dependency — both are transitive deps of the already-declared
    `@polkadot/api@^16.4.6`, pulled in for light-client support. So the lockfile is pruned or
    was generated inconsistently; it is not a `package.json` problem.

    Regenerate with the minimal-diff approach first:

    1. `cd frontend && npm install --package-lock-only`. This rewrites the lockfile only,
       leaving `node_modules` untouched, and fills in the missing resolved entries while
       preserving existing pins.
    2. Inspect the result with `git diff --stat frontend/package-lock.json` and then read the
       diff. Confirm the added entries are the two expected packages plus any transitive deps
       they legitimately require. Every added entry must carry a `resolved` URL pointing at
       `registry.npmjs.org` and an `integrity` hash.
    3. Reject unrelated churn. If the diff bumps versions of packages unrelated to this fix,
       reset and retry with `npm install --package-lock-only --prefer-offline`, or pin the
       offending entries back. The goal is the smallest diff that makes `npm ci` work.
    4. If `--package-lock-only` does not resolve the `EUSAGE`, fall back to a full
       `npm install`, then re-apply the same diff scrutiny.
    5. Leave `package.json` unchanged. Nothing here requires a manifest edit — both packages
       are transitive. If you somehow conclude otherwise, stop and report rather than editing it.

    Note a real environment difference: local Node is v26 / npm 11, while CI pins Node 20
    (npm 10). `lockfileVersion: 3` is readable by npm 10, so this is expected to be fine, but
    it is the most likely source of a local-passes/CI-fails divergence. Task 3's CI watch is
    what actually confirms it.
  </action>
  <verify>
    <automated>cd frontend &amp;&amp; npm ci 2>&amp;1 | tail -15</automated>
    <automated>cd frontend &amp;&amp; npm run lint 2>&amp;1 | tail -15</automated>
    <automated>cd frontend &amp;&amp; npm run build 2>&amp;1 | tail -15</automated>
    <automated>cd frontend &amp;&amp; npx vitest run 2>&amp;1 | tail -15</automated>
    <automated>cd /Volumes/WorkFiles/Projects/InkTix_hackathon_version &amp;&amp; git diff --name-only -- frontend/package.json</automated>
  </verify>
  <done>
    - `npm ci` completes with no `EUSAGE` and no missing-from-lock-file error.
    - `npm run lint`, `npm run build`, and `npx vitest run` all exit 0 (these are the exact
      three steps the CI frontend job runs after `npm ci`).
    - The lockfile diff adds resolved entries for the two named packages, each with a
      registry `resolved` URL and an `integrity` hash, and contains no unrelated version bumps.
    - The `package.json` diff check prints nothing.
  </done>
</task>

<task type="auto">
  <name>Task 3: Commit, push, and confirm the Actions run goes green</name>
  <files>contracts/inktix/src/**, frontend/package-lock.json</files>
  <action>
    Project CLAUDE.md mandates push-on-every-commit, so this task commits, pushes, and then
    actually watches the result rather than assuming it.

    1. Re-run both gates one final time on the combined working tree to make sure Task 1 and
       Task 2 did not interact: the contracts gate and the four frontend commands.
    2. Stage only the intended paths — the seven contract sources and
       `frontend/package-lock.json`. Do not sweep in the unrelated pre-existing dirty files
       (`.planning/STATE.md`, `.planning/config.json`, `.planning/state.json`,
       `.planning/milestone.lock`, the untracked doc in `docs/`); those belong to other work.
       Verify with `git status --short` before committing.
    3. Commit with a message that names both fixes and the mechanism, e.g. subject
       `fix(ci): clear -D warnings in contracts and resync frontend lockfile`, with a body
       that explains the setup-rust-toolchain RUSTFLAGS default, the pruned lockfile entries,
       and lists the three documented gaps as follow-ups. End the message with:
       `Co-Authored-By: Claude Opus 5 (1M context) <noreply@anthropic.com>`
    4. `git push origin redesign/polkadot-hub`.
    5. Find the new run and watch it to completion:
       `gh run list --repo RycBrownrigg/InkTix --branch redesign/polkadot-hub --limit 1`
       then `gh run watch <run-id> --repo RycBrownrigg/InkTix`. Confirm the run's head SHA
       matches the commit just pushed — if it does not, you are watching the old failed run.
    6. Report the final conclusion. If it succeeded, say so with the run URL. **If it failed,
       report exactly which job and step failed and paste the relevant log excerpt
       (`gh run view <run-id> --repo RycBrownrigg/InkTix --log-failed`). Do not start guessing
       at further fixes in this task** — hand the concrete failure back for a fresh decision.
  </action>
  <verify>
    <automated>cd /Volumes/WorkFiles/Projects/InkTix_hackathon_version &amp;&amp; git log origin/redesign/polkadot-hub -1 --oneline</automated>
    <automated>cd /Volumes/WorkFiles/Projects/InkTix_hackathon_version &amp;&amp; gh run list --repo RycBrownrigg/InkTix --branch redesign/polkadot-hub --limit 1 --json databaseId,headSha,status,conclusion,url</automated>
  </verify>
  <done>
    - Commit exists on `origin/redesign/polkadot-hub` containing exactly the contract source
      changes and the lockfile.
    - The `.planning/` and `docs/` files that were already dirty before this task remain
      uncommitted.
    - `gh run list` shows a run whose `headSha` equals the pushed commit, with
      `status: completed`.
    - The run's `conclusion` is reported verbatim. `success` completes the task; any other
      conclusion is reported with the failing job, step, and log excerpt, and the task stops
      there rather than attempting further fixes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → developer machine → repo | `npm install` resolves and pins third-party code into a committed lockfile |
| repo → GitHub Actions runner | Pushed code executes in CI with the repo token |
| frontend SDK → contract metadata | ink! message arg labels are a published interface the frontend binds to |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-g2j-01 | Tampering | `frontend/package-lock.json` regeneration | high | mitigate | Task 2 requires reading the lockfile diff and confirming every added entry has a `registry.npmjs.org` `resolved` URL plus an `integrity` hash, and rejecting unrelated version bumps |
| T-g2j-SC | Tampering | npm dependency resolution | medium | accept | No new direct dependencies are added. `@polkadot-api/substrate-client` and `smoldot` are pre-existing transitive deps of the already-trusted `@polkadot/api@^16.4.6` (smoldot is Parity's light client; `@polkadot-api` is the PAPI org). Legitimacy is inherited from the existing direct dep, so no new-package human gate applies; the diff review in T-g2j-01 is the control |
| T-g2j-02 | Tampering | ink! contract metadata | medium | mitigate | Task 1 forbids renaming `#[ink(message)]` parameters, using in-body discard bindings instead, so the published ABI arg labels the frontend consumes are unchanged |
| T-g2j-03 | Repudiation | Suppressed warnings hiding defects | high | mitigate | Gaps A/B/C get durable greppable `TODO(v2-gap)` markers and are escalated in the summary; the acceptance criteria assert the marker count, so a silent underscore-and-move-on fails the gate |
| T-g2j-04 | Elevation of Privilege | CI quality gate | high | mitigate | Editing `ci.yml`, `Cargo.toml`, `rust-toolchain.toml`, or `.cargo/config.toml` to relax `-D warnings` is explicitly prohibited and asserted by a protected-files diff check |
| T-g2j-05 | Denial of Service | Contract behavior regression | medium | mitigate | The contract change is constrained to behavior-neutral edits; the 14 unit tests must pass unmodified and the diff is reviewed for logic changes |
</threat_model>

<verification>
The CI job steps, reproduced locally in order:

```bash
# contracts job — RUSTFLAGS mirrors setup-rust-toolchain@v1's default
cd contracts/inktix
RUSTFLAGS="-D warnings" cargo test          # expect: test result: ok. 14 passed; 0 failed
RUSTFLAGS="-D warnings" cargo contract build

# frontend job
cd ../../frontend
npm ci
npm run lint
npm run build
npx vitest run
```

The local pinned toolchain (`nightly-2026-01-15`) and `cargo-contract 6.0.0-beta.2` are both
already installed, so the contracts job is fully reproducible locally. The frontend job is
reproducible modulo the Node 26 (local) vs Node 20 (CI) difference — which is why Task 3
watches the real run rather than trusting local green.
</verification>

<success_criteria>
- Both CI jobs pass on `redesign/polkadot-hub` for the pushed commit.
- Zero contract warnings under `-D warnings`, achieved without touching any pinned file.
- Contract diff is behavior-neutral; 14 unit tests pass unmodified.
- Three genuine logic gaps documented in code and escalated to the user as follow-up work,
  rather than silently suppressed.
- Lockfile diff is minimal, integrity-verified, and `package.json` is untouched.
</success_criteria>

<output>
Create `.planning/quick/260921-g2j-fix-ci-failures-on-redesign-polkadot-hub/260921-g2j-SUMMARY.md` when done.

The summary MUST include a **Follow-up work surfaced** section covering Gap A (parking-pass
stub persists nothing and increments the venue counter), Gap B (concession-credits message
drops the caller's `CurrencyId` for a hardcoded value; `CurrencyId` has no string conversion),
and Gap C (Sports `sport_type` never validated against team/season). For each: the file and
line, what is missing, what already exists to support it, and a suggested phase to schedule it.
</output>
